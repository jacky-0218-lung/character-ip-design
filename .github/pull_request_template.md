## Summary

<!-- What does this change and why? -->

## Checklist

- [ ] `python3 -m unittest discover -s tests -v` passes
- [ ] `python3 tools/check_repository.py` passes
- [ ] Updated `CHANGELOG.md` if skill files changed
- [ ] No famous-IP symbol combinations added; originality guardrail respected
- [ ] No local paths, large binaries, or generated outputs committed
